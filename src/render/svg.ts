import type { CalendarGrid, Theme } from "../types";

/**
 * Renders a heatmap calendar as SVG inside the given container.
 */
export function renderSvgCalendar(
  _container: HTMLElement,
  _model: CalendarGrid,
  _theme?: Theme,
): void {
  // TODO: Implement in STR-16
}
